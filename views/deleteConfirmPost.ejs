<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/css/tailwind.css" rel="stylesheet" />
    <link href="/css/style.css" rel="stylesheet" />
    <title>Delete Post - <%= post.title %></title>
  </head>
  <body class="bg-gray-50 min-h-screen">
    <!-- Navigation Bar -->
    <nav class="bg-white shadow-sm border-b border-gray-200">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <a href="/posts" class="text-2xl font-bold text-blue-600 hover:text-blue-700">📱 Reddit Clone</a>
          <a href="/posts" class="px-4 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition">
            ← Back to Home
          </a>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <!-- Warning Header -->
      <div class="bg-red-50 border-l-4 border-red-500 rounded-lg p-6 mb-8">
        <div class="flex items-center mb-4">
          <div class="text-4xl mr-4">⚠️</div>
          <div>
            <h1 class="text-2xl font-bold text-red-900">Confirm Deletion</h1>
            <p class="text-red-700">This action cannot be undone</p>
          </div>
        </div>
      </div>

      <!-- Post Preview Card -->
      <div class="bg-white rounded-lg shadow-lg overflow-hidden mb-6">
        <div class="bg-gray-100 px-6 py-4 border-b border-gray-200">
          <h2 class="text-lg font-semibold text-gray-900">Post to be deleted:</h2>
        </div>

        <div class="p-6">
          <h3 class="text-2xl font-bold text-gray-900 mb-4"><%= post.title %></h3>

          <div class="flex items-center space-x-3 text-sm text-gray-500 mb-4">
            <span class="flex items-center">
              👤 <strong class="ml-1 text-gray-700"><%= post.creator.uname %></strong>
            </span>
            <span>•</span>
            <span class="px-2 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-medium">
              r/<%= post.subgroup %>
            </span>
            <span>•</span>
            <span>📅 <%= post.timestamp.toLocaleDateString() %></span>
          </div>

          <% if (post.link) { %>
            <div class="mb-4">
              <a href="<%= post.link %>" target="_blank" class="inline-flex items-center px-3 py-2 bg-blue-50 text-blue-700 rounded-lg hover:bg-blue-100 transition text-sm">
                🔗 <%= post.link %>
              </a>
            </div>
          <% } %>

          <p class="text-gray-700 mb-4"><%= post.description %></p>

          <div class="pt-4 border-t border-gray-200">
            <p class="text-sm text-gray-600">
              <strong>💬 Comments:</strong> <%= post.comments.length %> comment<%= post.comments.length !== 1 ? 's' : '' %> will also be deleted
            </p>
          </div>
        </div>
      </div>

      <!-- Warning Message -->
      <div class="bg-yellow-50 border border-yellow-300 rounded-lg p-4 mb-6">
        <div class="flex">
          <div class="flex-shrink-0">
            <span class="text-2xl">🚨</span>
          </div>
          <div class="ml-3">
            <h3 class="text-sm font-bold text-yellow-900 mb-1">Warning: Permanent Deletion</h3>
            <p class="text-sm text-yellow-800">
              The post and all <%= post.comments.length %> associated comment<%= post.comments.length !== 1 ? 's' : '' %> will be permanently removed from the database. This action cannot be reversed.
            </p>
          </div>
        </div>
      </div>

      <!-- Action Buttons -->
      <div class="flex flex-col sm:flex-row gap-3">
        <form action="/posts/delete/<%= post.id %>" method="POST" class="flex-1">
          <button
            type="submit"
            class="w-full px-6 py-3 bg-red-600 text-white rounded-lg hover:bg-red-700 transition font-bold shadow-lg hover:shadow-xl flex items-center justify-center space-x-2"
          >
            <span>🗑️</span>
            <span>Yes, Delete Permanently</span>
          </button>
        </form>

        <a
          href="/posts/show/<%= post.id %>"
          class="flex-1 px-6 py-3 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition font-bold text-center flex items-center justify-center space-x-2"
        >
          <span>↩️</span>
          <span>Cancel & Go Back</span>
        </a>
      </div>

      <!-- Additional Info -->
      <div class="mt-6 text-center">
        <p class="text-sm text-gray-500">
          Changed your mind?
          <a href="/posts/show/<%= post.id %>" class="text-blue-600 hover:text-blue-800 font-semibold underline">
            Return to post
          </a>
        </p>
      </div>
    </main>
  </body>
</html>
