<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.classless.min.css"
    />
    <link href="/css/style.css" rel="stylesheet" />
    <title>Delete Post - <%= post.title %></title>
  </head>
  <body>
    <main>
      <div class="authOptions">
        <h1>Delete Post</h1>
        <a role="button" href="/posts">Home</a>
      </div>

      <!-- Confirmation Message -->
      <article>
        <header>
          <h2>Are you sure you want to delete this post?</h2>
        </header>

        <div style="background-color: #f0f0f0; padding: 1rem; border-radius: 5px; margin: 1rem 0;">
          <h3><%= post.title %></h3>
          <p>
            Posted by <strong><%= post.creator.uname %></strong> in
            <em><%= post.subgroup %></em> •
            <%= post.timestamp.toLocaleDateString() %>
          </p>
          <% if (post.link) { %>
          <p>
            <a href="<%= post.link %>" target="_blank"><%= post.link %></a>
          </p>
          <% } %>
          <p><%= post.description %></p>
          <p><strong>Comments:</strong> <%= post.comments.length %></p>
        </div>

        <p style="color: #d32f2f; font-weight: bold;">
          ⚠️ Warning: This action cannot be undone. The post and all its comments will be permanently deleted.
        </p>

        <footer>
          <div style="display: flex; gap: 1rem; justify-content: flex-start;">
            <!-- Delete Form -->
            <form action="/posts/delete/<%= post.id %>" method="POST" style="margin: 0;">
              <button type="submit" style="background-color: #d32f2f;">
                Yes, Delete Post
              </button>
            </form>

            <!-- Cancel Button -->
            <a
              role="button"
              href="/posts/show/<%= post.id %>"
              style="background-color: #757575;"
            >
              Cancel
            </a>
          </div>
        </footer>
      </article>
    </main>
  </body>
</html>
